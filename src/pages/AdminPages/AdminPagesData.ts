import { AdminPageSlugs } from "@models/pages/enums";
import { PageData } from "@models/pages/types";

export const AdminPagesData: PageData = {
    UNVERIFIED_PLACES: {
        name: "Неопубликованные площадки",
        slug: AdminPageSlugs.UNVERIFIED_PLACES
    }
};
