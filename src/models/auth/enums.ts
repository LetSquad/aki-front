export enum SignInFieldName {
    EMAIL = "email",
    PASSWORD = "password"
}

export enum ResetPasswordFieldName {
    EMAIL = "userEmail"
}

export enum BaseRegistrationFieldName {
    ROLE = "role",
    EMAIL = "email",
    PHONE = "phone",
    FIRST_NAME = "firstName",
    LAST_NAME = "lastName",
    SURNAME = "surname",
    PASSWORD = "password",
    PASSWORD_CONFIRM = "confirmPassword"
}

export enum LandlordRegistrationFieldName {
    INN = "inn",
    ENTITY_NAME = "entityName",
    JOB_TITLE = "jobTitle"
}
